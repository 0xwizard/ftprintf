#include <stdio.h>
#include <stdlib.h>
#include "ft_printf.h"
#include <limits.h>

int main()
{
    
    // char *str;

    // str = malloc(sizeof(char ) * 5);

    // void *ptr = (void *)31313131;

    // str[0] = 'a';
    // str[1] = 'd';
    // str[2] = 'e';
    // str[3] = 'm';
    // str[4] = '\0';
 

    // int first = printf("sa sa %p\n", str);
    // int second = ft_printf("sa sa %p\n", str);

    // printf("first: %d\n", first);
    // printf("second: %d\n", second);

    // first = printf("%x\n", ptr);
    // second = ft_printf("%x\n", ptr);

    // printf("first: %d\n", first);
    // printf("second: %d\n", second);

    return (0);
}